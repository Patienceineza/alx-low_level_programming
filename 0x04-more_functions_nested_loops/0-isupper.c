#include "main.h"
/**
* _isupper - function that checks uppercase characters
* @c: int member type
*Return: 1 if uppercase else 0
*/
int _isupper(int c)
{

if ((c >= 'A') && (c <= 'Z'))

return (1);
else

return (0);
}
